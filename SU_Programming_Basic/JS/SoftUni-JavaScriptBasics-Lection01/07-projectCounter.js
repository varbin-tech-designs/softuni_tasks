function projectCounter(name, project) {
    let totalHours = project * 3;

    console.log(`The architect ${name} will need ${totalHours} hours to complete ${project} project/s.`);
    
}

projectCounter('George', 4);